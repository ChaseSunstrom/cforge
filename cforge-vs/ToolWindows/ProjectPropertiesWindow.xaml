<UserControl x:Class="CforgeVS.ProjectPropertiesWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource VsBrush.WindowText}"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,2,0,8"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,2,0,8"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Foreground" Value="{DynamicResource VsBrush.WindowText}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Foreground" Value="{DynamicResource VsBrush.WindowText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource VsBrush.ActiveBorder}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="4,4"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="12,8">
            <TextBlock x:Name="ProjectNameHeader" Text="Project Properties" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock x:Name="ProjectPathText" Text="" FontSize="11" Opacity="0.7"/>
        </StackPanel>

        <!-- Properties Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="12,0">
            <StackPanel>
                <!-- Project Section -->
                <GroupBox Header="Project">
                    <StackPanel>
                        <TextBlock Text="Name"/>
                        <TextBox x:Name="ProjectNameBox"/>

                        <TextBlock Text="Version"/>
                        <TextBox x:Name="ProjectVersionBox"/>

                        <TextBlock Text="Description"/>
                        <TextBox x:Name="ProjectDescriptionBox" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>

                        <TextBlock Text="Binary Type"/>
                        <ComboBox x:Name="BinaryTypeCombo">
                            <ComboBoxItem Content="executable"/>
                            <ComboBoxItem Content="static_library"/>
                            <ComboBoxItem Content="shared_library"/>
                            <ComboBoxItem Content="header_only"/>
                        </ComboBox>

                        <TextBlock Text="C++ Standard"/>
                        <ComboBox x:Name="CppStandardCombo">
                            <ComboBoxItem Content="11"/>
                            <ComboBoxItem Content="14"/>
                            <ComboBoxItem Content="17"/>
                            <ComboBoxItem Content="20"/>
                            <ComboBoxItem Content="23"/>
                        </ComboBox>

                        <TextBlock Text="C Standard"/>
                        <ComboBox x:Name="CStandardCombo">
                            <ComboBoxItem Content="99"/>
                            <ComboBoxItem Content="11"/>
                            <ComboBoxItem Content="17"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <!-- Build Section -->
                <GroupBox Header="Build">
                    <StackPanel>
                        <TextBlock Text="Output Directory"/>
                        <TextBox x:Name="OutputDirBox"/>

                        <TextBlock Text="Source Directories (comma-separated)"/>
                        <TextBox x:Name="SourceDirsBox"/>

                        <TextBlock Text="Include Directories (comma-separated)"/>
                        <TextBox x:Name="IncludeDirsBox"/>

                        <CheckBox x:Name="ExportCompileCommandsCheck" Content="Export compile_commands.json"/>
                    </StackPanel>
                </GroupBox>

                <!-- Debug Configuration -->
                <GroupBox Header="Debug Configuration">
                    <StackPanel>
                        <TextBlock Text="Optimization"/>
                        <ComboBox x:Name="DebugOptimizeCombo">
                            <ComboBoxItem Content="debug"/>
                            <ComboBoxItem Content="none"/>
                            <ComboBoxItem Content="speed"/>
                        </ComboBox>

                        <CheckBox x:Name="DebugInfoCheck" Content="Generate Debug Info" IsChecked="True"/>

                        <TextBlock Text="Preprocessor Defines (comma-separated)"/>
                        <TextBox x:Name="DebugDefinesBox"/>

                        <TextBlock Text="Warnings"/>
                        <ComboBox x:Name="DebugWarningsCombo">
                            <ComboBoxItem Content="all"/>
                            <ComboBoxItem Content="extra"/>
                            <ComboBoxItem Content="pedantic"/>
                            <ComboBoxItem Content="none"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <!-- Release Configuration -->
                <GroupBox Header="Release Configuration">
                    <StackPanel>
                        <TextBlock Text="Optimization"/>
                        <ComboBox x:Name="ReleaseOptimizeCombo">
                            <ComboBoxItem Content="speed"/>
                            <ComboBoxItem Content="size"/>
                            <ComboBoxItem Content="aggressive"/>
                            <ComboBoxItem Content="none"/>
                        </ComboBox>

                        <CheckBox x:Name="ReleaseLtoCheck" Content="Enable Link Time Optimization (LTO)"/>

                        <TextBlock Text="Preprocessor Defines (comma-separated)"/>
                        <TextBox x:Name="ReleaseDefinesBox"/>
                    </StackPanel>
                </GroupBox>

                <!-- Testing Section -->
                <GroupBox Header="Testing">
                    <StackPanel>
                        <CheckBox x:Name="TestEnabledCheck" Content="Enable Testing"/>

                        <TextBlock Text="Test Framework"/>
                        <ComboBox x:Name="TestFrameworkCombo">
                            <ComboBoxItem Content="catch2"/>
                            <ComboBoxItem Content="gtest"/>
                            <ComboBoxItem Content="doctest"/>
                            <ComboBoxItem Content="boost"/>
                        </ComboBox>

                        <TextBlock Text="Test Directory"/>
                        <TextBox x:Name="TestDirBox"/>
                    </StackPanel>
                </GroupBox>

                <!-- Package Section -->
                <GroupBox Header="Package">
                    <StackPanel>
                        <TextBlock Text="Authors (comma-separated)"/>
                        <TextBox x:Name="AuthorsBox"/>

                        <TextBlock Text="License"/>
                        <TextBox x:Name="LicenseBox"/>

                        <TextBlock Text="Homepage URL"/>
                        <TextBox x:Name="HomepageBox"/>

                        <TextBlock Text="Repository URL"/>
                        <TextBox x:Name="RepositoryBox"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12,8">
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click"/>
            <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click"/>
            <Button x:Name="OpenTomlButton" Content="Edit cforge.toml" Click="OpenTomlButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
