# Workspace CMakeLists.txt for hi
# Generated by cforge - C++ project management tool

cmake_minimum_required(VERSION 3.14)

# Workspace configuration
project(hi LANGUAGES CXX)

# Workspace description
set(WORKSPACE_DESCRIPTION "A C++ workspace created with cforge")

# Common build settings
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Build configuration
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE "Debug")
endif()

message(STATUS "Building workspace with ${CMAKE_BUILD_TYPE} configuration")

# Set output directories
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)

find_package(Threads REQUIRED)

# Add all projects in the workspace
# Project: proj2:proj2:true
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/proj2:proj2:true/CMakeLists.txt")
    add_subdirectory(proj2:proj2:true)
else()
    message(WARNING "Project proj2:proj2:true has no CMakeLists.txt file")
endif()

# Project: proj1:proj1:true
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/proj1:proj1:true/CMakeLists.txt")
    add_subdirectory(proj1:proj1:true)
else()
    message(WARNING "Project proj1:proj1:true has no CMakeLists.txt file")
endif()

